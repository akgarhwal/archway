import type { Category, Mix, RequestKind, ServiceDef, ServiceId } from '../types';

export const CATALOG: Record<ServiceId, ServiceDef> = {
  internet: {
    id: 'internet',
    name: 'Users / Internet',
    short: 'Internet',
    aws: 'Public clients',
    category: 'source',
    blurb: 'Every request is born here — customers, bots, and attackers share the same wire.',
    when: 'Always on the canvas. You never own this. You only decide what it can reach.',
    capex: 0,
    opexPerMin: 0,
    rps: 10_000,
    latency: 0,
    color: '#5ce1e6',
    placeable: false,
    ports: true,
  },
  route53: {
    id: 'route53',
    name: 'Route 53',
    short: 'DNS',
    aws: 'Amazon Route 53',
    category: 'edge',
    blurb: 'DNS with health checks. Sends users to a live edge, not a dead box.',
    when: 'Use when you have more than one entry (failover) or want latency-based routing later.',
    capex: 180,
    opexPerMin: 1.2,
    rps: 800,
    latency: 2,
    color: '#8b7cf6',
    placeable: true,
    ports: true,
  },
  shield: {
    id: 'shield',
    name: 'Shield',
    short: 'Shield',
    aws: 'AWS Shield',
    category: 'security',
    blurb: 'L3/L4 DDoS absorption. Stops volumetric floods before they become an EC2 bill.',
    when: 'Put this at the edge, in front of CloudFront or ALB. It will not catch SQL injection.',
    capex: 820,
    opexPerMin: 6.4,
    rps: 420,
    latency: 3,
    color: '#ff6b6b',
    placeable: true,
    ports: true,
  },
  waf: {
    id: 'waf',
    name: 'WAF',
    short: 'WAF',
    aws: 'AWS WAF',
    category: 'security',
    blurb: 'Layer-7 firewall. Inspects HTTP for SQLi, XSS, bots, and rate abuse.',
    when: 'Always in front of any origin that parses input. Undersized WAFs start letting attackers through.',
    capex: 640,
    opexPerMin: 5.2,
    rps: 110,
    latency: 4,
    color: '#ff8a5b',
    placeable: true,
    ports: true,
  },
  cloudfront: {
    id: 'cloudfront',
    name: 'CloudFront',
    short: 'CDN',
    aws: 'Amazon CloudFront',
    category: 'edge',
    blurb: 'Global CDN. Cache hits never touch your VPC. Also soaks a slice of L7 floods.',
    when: 'Static assets, public reads, and as a buffer in front of ALB. Hit ratio is the KPI.',
    capex: 920,
    opexPerMin: 7.8,
    rps: 260,
    latency: 7,
    color: '#c084fc',
    placeable: true,
    ports: true,
  },
  apigateway: {
    id: 'apigateway',
    name: 'API Gateway',
    short: 'APIGW',
    aws: 'Amazon API Gateway',
    category: 'edge',
    blurb: 'Managed front door: routing, auth, throttling. Not a static file server.',
    when: 'Public APIs, especially in front of Lambda. Throttle is cheaper than a melted origin.',
    capex: 520,
    opexPerMin: 4.1,
    rps: 90,
    latency: 6,
    color: '#f472b6',
    placeable: true,
    ports: true,
  },
  alb: {
    id: 'alb',
    name: 'Load Balancer',
    short: 'ALB',
    aws: 'Elastic Load Balancing (ALB)',
    category: 'compute',
    blurb: 'Spreads HTTP across healthy targets. Does not inspect or block attacks.',
    when: 'Two or more EC2/Lambda targets. One instance behind an ALB is still a single point of failure.',
    capex: 460,
    opexPerMin: 3.4,
    rps: 160,
    latency: 3,
    color: '#f5a524',
    placeable: true,
    ports: true,
  },
  ec2: {
    id: 'ec2',
    name: 'EC2',
    short: 'EC2',
    aws: 'Amazon EC2',
    category: 'compute',
    blurb: 'Virtual servers. Finite CPU. DDoS that lands here is a bill and an outage.',
    when: 'Long-running app servers. Upgrade size or add instances — don’t point the Internet at one box forever.',
    capex: 720,
    opexPerMin: 7.2,
    rps: 14,
    latency: 16,
    color: '#f59e0b',
    placeable: true,
    upgradable: true,
    ports: true,
  },
  lambda: {
    id: 'lambda',
    name: 'Lambda',
    short: 'Lambda',
    aws: 'AWS Lambda',
    category: 'compute',
    blurb: 'Serverless compute that scales with demand — including attacker demand.',
    when: 'Bursty, spiky work behind a throttle. Naked Lambda + DDoS is how bills become incidents.',
    capex: 380,
    opexPerMin: 1.8,
    rps: 48,
    latency: 11,
    color: '#fb923c',
    placeable: true,
    upgradable: true,
    ports: true,
  },
  cache: {
    id: 'cache',
    name: 'ElastiCache',
    short: 'Cache',
    aws: 'Amazon ElastiCache',
    category: 'data',
    blurb: 'In-memory cache. Hits are microseconds. Misses (and stampedes) are the database’s problem.',
    when: 'Hot reads. Sit it next to compute, never on the public internet.',
    capex: 680,
    opexPerMin: 5.4,
    rps: 95,
    latency: 2,
    color: '#38bdf8',
    placeable: true,
    ports: true,
  },
  rds: {
    id: 'rds',
    name: 'RDS',
    short: 'RDS',
    aws: 'Amazon RDS',
    category: 'data',
    blurb: 'Managed SQL. Source of truth, low QPS, expensive. A public RDS is a breach waiting for a packet.',
    when: 'Relational writes and reads that must be consistent. Hide it behind compute. Add a cache.',
    capex: 1280,
    opexPerMin: 10.5,
    rps: 10,
    latency: 22,
    color: '#3b82f6',
    placeable: true,
    upgradable: true,
    ports: true,
  },
  dynamodb: {
    id: 'dynamodb',
    name: 'DynamoDB',
    short: 'DDB',
    aws: 'Amazon DynamoDB',
    category: 'data',
    blurb: 'Managed NoSQL with higher QPS than RDS. Hot keys still want a cache.',
    when: 'Key-value and session data at scale. Still not a public endpoint.',
    capex: 940,
    opexPerMin: 8.2,
    rps: 28,
    latency: 8,
    color: '#60a5fa',
    placeable: true,
    ports: true,
  },
  s3: {
    id: 's3',
    name: 'S3',
    short: 'S3',
    aws: 'Amazon S3',
    category: 'data',
    blurb: 'Object storage. Cheap, durable, high RPS. Perfect origin for CloudFront. Not a database.',
    when: 'Static assets and uploads. Pair with CloudFront so the bucket is not the front door.',
    capex: 280,
    opexPerMin: 1.9,
    rps: 90,
    latency: 11,
    color: '#f43f5e',
    placeable: true,
    ports: true,
  },
  sqs: {
    id: 'sqs',
    name: 'SQS',
    short: 'SQS',
    aws: 'Amazon SQS',
    category: 'integration',
    blurb: 'Queue. Accepts writes in milliseconds (202) and lets workers catch up. Overflow still drops.',
    when: 'Spiky writes, uploads, anything the user does not need to wait on. Needs a consumer.',
    capex: 340,
    opexPerMin: 2.4,
    rps: 120,
    latency: 3,
    color: '#e879f9',
    placeable: true,
    ports: true,
  },
  cloudwatch: {
    id: 'cloudwatch',
    name: 'CloudWatch',
    short: 'Watch',
    aws: 'Amazon CloudWatch',
    category: 'observe',
    blurb: 'Metrics, alarms, traces. Not on the request path — on the operator path.',
    when: 'The moment you care about saturation before users do. Unlocks earlier coach warnings and X-Ray samples.',
    capex: 240,
    opexPerMin: 1.6,
    rps: 0,
    latency: 0,
    color: '#94a3b8',
    placeable: true,
    ports: false,
  },
};

export const CATEGORY_ORDER: { id: Category; label: string }[] = [
  { id: 'edge', label: 'Edge & DNS' },
  { id: 'security', label: 'Security' },
  { id: 'compute', label: 'Compute & scale' },
  { id: 'data', label: 'Data' },
  { id: 'integration', label: 'Integration' },
  { id: 'observe', label: 'Observability' },
];

export const KIND_META: Record<
  RequestKind,
  { label: string; hint: string; earn: number }
> = {
  static: { label: 'STATIC', hint: 'CSS, images, JS — CDN / S3', earn: 0.1 },
  read: { label: 'READ', hint: 'GET /api — cache then DB', earn: 0.18 },
  write: { label: 'WRITE', hint: 'POST — queue or DB', earn: 0.42 },
  upload: { label: 'UPLOAD', hint: 'PUT object — S3', earn: 0.48 },
  search: { label: 'SEARCH', hint: 'Heavy query — compute + DB', earn: 0.24 },
  malicious: { label: 'ATTACK', hint: 'SQLi / XSS — block at WAF', earn: 0.04 },
  ddos: { label: 'DDOS', hint: 'Flood — absorb at Shield / CDN', earn: 0.04 },
};

/** Packet and mix-bar colors — keep these in lockstep with `.pkt-*` in CSS. */
export const KIND_COLOR: Record<RequestKind, string> = {
  static: '#7dd3fc',
  read: '#86efac',
  write: '#fbbf24',
  upload: '#c4b5fd',
  search: '#67e8f9',
  malicious: '#fb7185',
  ddos: '#f43f5e',
};

export const DEFAULT_MIX: Mix = {
  static: 0.38,
  read: 0.28,
  write: 0.12,
  upload: 0.08,
  search: 0.06,
  malicious: 0.06,
  ddos: 0.02,
};

/** Production mix is meaner. Attacks share the wire from the first second. */
export const LIVE_MIX: Mix = {
  static: 0.3,
  read: 0.26,
  write: 0.14,
  upload: 0.07,
  search: 0.06,
  malicious: 0.11,
  ddos: 0.06,
};

/** Same grant in every mode — ten EC2s, which is the trap. */
export const START_GRANT = 7200;
export const LIVE_START_MONEY = START_GRANT;
/** Opening Production load. Same $ per packet as missions; this RPS is what keeps a fortress from printing money on day one. */
export const LIVE_START_RPS = 6;

export const SIZE_LABEL = ['', 'T1', 'T2', 'T3'] as const;
export const SIZE_CAP = [0, 1, 2, 4] as const;
export const SIZE_OPEX = [0, 1, 1.8, 3.2] as const;
export const UPGRADE_COST = [0, 0, 620, 980] as const;
